@model IEnumerable<Flex_SGM.Models.ApplicationUser>

@{
    ViewBag.Title = "Index";
}

<h2>Usuarios</h2>

<h3>
    @if (ViewBag.super == true)
    {
        @Html.ActionLink("Crear Nuevo", "Create")
    }
</h3>
<div class="row clearfix">
    <center> @{Html.RenderPartial("_Result");}</center>
</div>
<h3>
    A los usuarios nuevos: Se tiene que Realizar la asignación de los roles de manera manual, asignarlos al Rol/Área a la cual pertenecen para que puedan tener acceso y hacer uso de las funciones....
</h3>
<h2>Usuarios sin Rol</h2>
<table class="tabled table-bordered table-striped" id="table_id" style="width:100%">
    <thead>
        <tr>
            <th>
                Usuario
            </th>
            <th>
                Nombre Completo
            </th>
            <th>
                Nomina
            </th>
            <th>
                Departamento
            </th>
            <th>
                Metrico
            </th>
            <th>
                @Html.Label("Menu")
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in @ViewBag.Usernorole)
        {
            <tr>
                <td>
                    @item.UserName
                </td>
                <td>
                    @item.UserFullName
                </td>
                <td>
                    @item.Nomina
                </td>
                <td>
                    @item.Departamento
                </td>
                <td>
                    @item.Area
                </td>
                <td>
                    @if (ViewBag.super == true)
                    {
                        @Html.ActionLink("Editar", "Edit", new { id = item.Id })
                    } |
                    @Html.ActionLink("Detalles", "Details", new { id = item.Id }) |
                    @if (ViewBag.super == true)
                    {
                        @Html.ActionLink("Eliminar", "Delete", new { id = item.Id })
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
<hr />
<h2>Usuarios con Rol</h2>
<table class="tabled table-bordered table-striped" id="table_id4" style="width:100%">
    <thead>
        <tr>
            <th>
                Usuario
            </th>
            <th>
                Nombre Completo
            </th>
            <th>
                Nomina
            </th>
            <th>
                Departamento
            </th>
            <th>
                Metrico
            </th>
            <th>
                @Html.Label("Menu")
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserFullName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nomina)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Departamento)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Area)
                </td>
                <td>
                    @if (ViewBag.super == true)
                    {
                        @Html.ActionLink("Editar", "Edit", new { id = item.Id })
                    } |
                    @Html.ActionLink("Detalles", "Details", new { id = item.Id }) |
                    @if (ViewBag.super == true)
                    {
                        @Html.ActionLink("Eliminar", "Delete", new { id = item.Id })
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

